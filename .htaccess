# Very simple dotfiles security for apache webserver - do not remove.
# Restrict access to DotFiles (like .htaccess, .env, .gitignore....) 
<FilesMatch "^\.">
    Order allow,deny
    Deny from all
</FilesMatch>
